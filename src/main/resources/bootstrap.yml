server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/ahiruzuy/msa_study_config
#          refresh-rate: 10
#          search-paths: classpath:/config

management:
  endpoints:
    web:
      exposure:
        include: '*'

#management:
#  endpoints:
#    web:
#      exposure:
#        include: '*'

#spring:
#  cloud:
#    config:
#      server:
#        git:
#          uri: https://github.com/ahiruzuy/msa_study_config/
#          private-key: 8aab8f7ca6a9332eff840474a8c0b70c130ab0db
#          username: config